# GCT CORE — PERSISTENCE RECONCILIATION BATCH 3G
## Read-Only Reservation Contract Reconciliation Review

## 1. Document Control

| Field | Value |
|---|---|
| Document ID | PERSISTENCE-B3G-RESERVATION-REVIEW |
| Title | Reservation Persistence Contract Reconciliation |
| Project | GCT Core |
| Type | Read-Only Contract / Architecture Review |
| Status | Review Only |
| Governing Process | GOV-DEV-001-DEVELOPMENT-PROCESS |
| Predecessor | PERSISTENCE-B3F-RESERVATION |
| Current Lint Warnings | 11 |
| Implementation Authorised | NO |

---

## 2. Purpose

Determine whether the active Reservation domain/application contracts provide sufficient information to support the physical Reservation model.

The review SHALL resolve only the following contract questions:

1. Reservation number ownership.
2. Booking persistence context.
3. Traveller snapshot mapping.
4. Reservation lifecycle ownership and mapping.
5. Reservation save context.
6. Reservation retrieval context.

No physical implementation is authorised by this review.

---

## 3. Governing Process

Follow:

`GOV-DEV-001-DEVELOPMENT-PROCESS`

Required sequence:

Read-only investigation
→ Contract reconciliation findings
→ Accepted contract decisions
→ Focused implementation specification
→ Architect Approval
→ Copilot Implementation

This review MUST NOT modify:

- source;
- tests;
- Prisma schema;
- migrations;
- database;
- configuration;
- repository contracts;
- domain contracts.

---

# 4. Decision 1 — Reservation Number

## Question

Determine where the canonical GCT Reservation number belongs in the active domain/application model.

## Required Investigation

Inspect:

- Reservation aggregate;
- Reservation identity;
- Reservation creation service;
- Reservation commands;
- Reservation DTOs;
- Reservation presenter/API representation;
- repository interface;
- existing Booking `bookingNumber`.

Determine whether the business concept is:

- domain-owned;
- application-owned;
- persistence-only;
- not currently represented.

## Required Finding

The review MUST explicitly determine:

- whether Reservation number is part of the Reservation domain contract;
- whether it is generated before persistence;
- whether it is generated by persistence;
- whether `Booking.bookingNumber` is the physical representation;
- how it is returned after creation/retrieval.

## Decision Classification

Choose one:

- `DOMAIN PROPERTY`
- `APPLICATION/PERSISTENCE PROPERTY`
- `PERSISTENCE-GENERATED`
- `CONTRACT GAP`

Do not change the domain during this review.

---

# 5. Decision 2 — Booking Persistence Context

## Question

Determine how the values required by Prisma `Booking` enter Reservation persistence.

Current Booking requirements include:

- `customerId`;
- `bookingStatusId`;
- `currencyId`;
- booking dates;
- booking number;
- total amount.

The active `Reservation` aggregate does not necessarily contain all of these values.

## Required Investigation

Trace each value from:

Domain
→ Application
→ Reservation service
→ Repository
→ Persistence.

For each field determine its authoritative source.

## Required Matrix

| Booking Field | Required | Current Source | Decision |
|---|---|---|---|
| `id` | YES | Reservation identity | TBD |
| `bookingNumber` | YES | Reservation/application | TBD |
| `customerId` | YES | Application/customer context | TBD |
| `bookingStatusId` | YES | Booking infrastructure | TBD |
| `currencyId` | YES | Reservation price/currency | TBD |
| booking dates | YES | Reservation/Journey | TBD |
| `totalAmount` | YES | Reservation pricing | TBD |

The review MUST replace every `TBD` with a supported finding or identify a contract gap.

---

# 6. Decision 3 — Booking Context Ownership

## Question

Determine whether Booking-specific values belong:

- inside the Reservation aggregate;
- inside an application persistence context;
- inside repository infrastructure;
- or in another existing application contract.

The review MUST NOT assume that the domain Reservation should contain every Prisma Booking field.

## Required Principle

The physical model MUST NOT force infrastructure fields into the domain solely because Prisma requires them.

If a persistence context is required, determine:

- its ownership;
- its fields;
- when it is created;
- which application service supplies it;
- how it reaches the repository.

---

# 7. Decision 4 — Traveller Snapshot Mapping

## Question

The active Reservation contains:

`travellerSnapshots[]`

The previous 3F specification incorrectly assumed a single:

`Booking.travellerSnapshot`

This assumption MUST NOT be carried forward.

## Required Investigation

Inspect:

- Reservation aggregate;
- Traveller snapshot value/object;
- Reservation creation;
- Reservation commands;
- Reservation mapper;
- API contracts;
- existing persistence structures.

Determine:

- whether multiple Traveller snapshots are valid;
- what identifies each snapshot;
- whether order matters;
- whether a snapshot represents one Traveller or a group;
- whether the full collection belongs to the Reservation root.

## Required Physical Decision

The physical model MUST preserve the cardinality of the active domain contract.

If the domain contains `travellerSnapshots[]`, the physical representation MUST support the same cardinality unless an approved application transformation exists.

## Decision Classification

Choose one:

- `ONE-TO-ONE SNAPSHOT`
- `ONE-TO-MANY SNAPSHOTS`
- `SNAPSHOT COLLECTION WITH ORDER`
- `CONTRACT GAP`

Do not modify Traveller or Customer.

---

# 8. Decision 5 — Traveller Snapshot Ownership

Determine whether Traveller snapshots are:

- immutable Reservation state;
- references to Traveller;
- application DTOs only;
- a combination of reference and snapshot.

The review MUST distinguish:

**Traveller aggregate**

from:

**Traveller snapshot captured by Reservation**.

The Reservation snapshot MUST NOT be retroactively changed when the Traveller aggregate changes.

---

# 9. Decision 6 — Reservation Lifecycle Ownership

## Question

Determine the authoritative owner of the GCT Reservation lifecycle.

The active domain lifecycle includes:

- `CREATED`;
- `QUOTED`;
- `CONFIRMED`;
- `AMENDED`;
- `CANCELLED`;
- `COMPLETED`.

The current physical Booking status does not contain all these values.

The supplier operational Reservation status is a separate concern.

## Required Investigation

Inspect:

- Reservation aggregate status;
- Reservation services;
- status value objects/enums;
- Booking status;
- supplier Reservation status;
- application commands;
- repository mappings.

## Required Decision

Explicitly distinguish:

1. GCT Reservation lifecycle.
2. Commercial Booking status.
3. Supplier operational Reservation status.

They MUST NOT be treated as equivalent without evidence.

---

# 10. Decision 7 — Lifecycle Physical Representation

Determine where the GCT Reservation lifecycle is physically persisted.

Possible outcomes:

- existing Booking status;
- dedicated Booking Reservation status;
- Reservation-owned lifecycle field;
- another existing physical representation.

The review MUST select one based on actual project contracts.

If a dedicated lifecycle field is required, define:

- ownership;
- values;
- mapping direction;
- transition authority.

Do not modify Prisma during this review.

---

# 11. Decision 8 — Reservation Save Context

## Question

Determine exactly what information the Reservation repository requires to persist a Reservation.

The current contract is:

`IReservationRepository.save(Reservation)`

The review MUST determine whether this is sufficient.

If not, identify the required persistence context without implementing it.

## Required Context Categories

Consider:

- Booking identity;
- reservation number;
- Customer identity;
- Booking status;
- currency;
- booking dates;
- Reservation lifecycle;
- Traveller snapshots;
- Journey snapshot;
- accommodation snapshots;
- pricing;
- supplier state.

---

# 12. Save Context Decision

If additional context is required, define conceptually:

`ReservationPersistenceContext`

The review MUST determine:

- whether such a context is required;
- who owns it;
- which fields it contains;
- whether it is application-layer or infrastructure-layer;
- whether it is required on save only or save/retrieval.

Do not create the type during this review.

---

# 13. Decision 9 — Reservation Retrieval Context

Determine whether Reservation retrieval requires information outside the physical Booking graph.

Inspect:

- `findById`;
- `findByNumber`;
- repository interface;
- mapper;
- application services.

Determine the minimum physical query graph required to reconstruct the active Reservation.

The retrieval contract MUST be capable of reconstructing:

- identity;
- lifecycle;
- Traveller snapshots;
- Journey snapshot;
- accommodation snapshots;
- pricing;
- supplier references;
- timeline/metadata.

---

# 14. Decision 10 — Create and Update Semantics

Determine whether the same persistence context is appropriate for:

- create;
- update;
- retrieval.

Identify any differences.

In particular determine:

- when reservation number exists;
- when Booking identity exists;
- when lifecycle exists;
- when Traveller snapshots are captured;
- when Journey snapshot is captured;
- when pricing becomes authoritative.

---

# 15. Decision 11 — Domain Contract Preservation

The physical model MUST adapt to the active domain/application contracts.

The review MUST NOT recommend:

- adding Prisma fields directly to the domain;
- replacing snapshots with database relationships without an approved domain decision;
- removing Traveller snapshots;
- removing Journey snapshots;
- adding infrastructure-specific fields to Reservation.

Any required contract change MUST be explicitly identified as a contract decision.

---

# 16. Decision 12 — Existing Booking Fields

The review MUST classify each existing Booking field required by persistence as:

- `DOMAIN-SUPPLIED`
- `APPLICATION-SUPPLIED`
- `PERSISTENCE-DERIVED`
- `INFRASTRUCTURE DEFAULT`
- `CONTRACT GAP`

At minimum review:

- `id`;
- `bookingNumber`;
- `customerId`;
- `bookingStatusId`;
- `currencyId`;
- booking dates;
- `totalAmount`;
- itinerary/package relationship.

---

# 17. Decision 13 — Reservation Number Lifecycle

Determine the lifecycle of the Reservation number:

1. When is it created?
2. Who generates it?
3. Is it part of the create command?
4. Can it be generated by persistence?
5. Is it returned by the create service?
6. Is it immutable?
7. Is it used for external lookup?

The review MUST establish a single authoritative answer.

---

# 18. Decision 14 — Traveller Snapshot Lifecycle

Determine:

1. When snapshots are created.
2. Whether they can be amended.
3. Whether multiple snapshots can exist.
4. Whether ordering matters.
5. Whether snapshots are immutable after confirmation.
6. How snapshots are reconstructed.

Do not infer these rules from Prisma.

Use the active application/domain contracts.

---

# 19. Decision 15 — Journey Snapshot Lifecycle

Determine:

1. When the Journey snapshot is captured.
2. Whether it is immutable.
3. Whether it can be amended.
4. What minimum information is required for reconstruction.
5. Whether the snapshot is singular or versioned.

Do not create a Journey persistence model.

---

# 20. Decision 16 — Pricing Contract

Confirm the source of:

- final GCT amount;
- currency;
- supplier pricing;
- pricing basis.

Determine whether the Reservation aggregate already contains enough information to persist these values.

Identify any missing application context.

---

# 21. Decision 17 — Accommodation Contract

Confirm the active Reservation accommodation snapshot contract:

- stops;
- ordering;
- property;
- rooms;
- rates;
- occupancy;
- child ages;
- supplier pricing;
- supplier references;
- booking state.

The review MUST use the actual domain/application representation rather than inventing a new one.

---

# 22. Contract Reconciliation Matrix

The completed review MUST produce:

| Concept | Domain/Application Source | Physical Requirement | Result |
|---|---|---|---|
| Reservation identity | Reservation identity | Booking.id | TBD |
| Reservation number | Current contract | Booking.bookingNumber | TBD |
| Customer | Application context | Booking.customerId | TBD |
| Currency | Reservation price | Booking.currencyId | TBD |
| Booking dates | Reservation/Journey | Booking dates | TBD |
| GCT lifecycle | Reservation status | Dedicated/Booking status | TBD |
| Traveller snapshots | Reservation | Traveller snapshot storage | TBD |
| Journey snapshot | Reservation | Journey snapshot storage | TBD |
| Final GCT price | Reservation pricing | Booking total/currency | TBD |
| Accommodation | Reservation snapshots | Stop/room structures | TBD |

No `TBD` values may remain in the final review unless explicitly classified as `CONTRACT GAP` or `DEPENDENCY`.

---

# 23. Required Findings

The final review MUST explicitly answer:

### Reservation Number

Where does it live and who owns it?

### Booking Context

Where do `customerId`, Booking status, currency and dates originate?

### Traveller

How does `travellerSnapshots[]` map to persistence?

### Lifecycle

Which lifecycle is authoritative and where is it persisted?

### Save

What exact context is required by `save()`?

### Retrieval

What exact information is required to reconstruct Reservation?

---

# 24. Contract Gap Classification

Every unresolved issue MUST be classified as one of:

- `RESOLVED`
- `EXISTING CONTRACT SUFFICIENT`
- `APPLICATION CONTRACT EXTENSION REQUIRED`
- `REPOSITORY CONTRACT EXTENSION REQUIRED`
- `PERSISTENCE CONTEXT REQUIRED`
- `DEPENDENCY`
- `BLOCKED`

Do not resolve a contract gap by changing code during this review.

---

# 25. Read-Only Restrictions

This review MUST NOT:

- modify source;
- modify tests;
- modify domain contracts;
- modify application contracts;
- modify repository interfaces;
- modify Prisma schema;
- generate migrations;
- modify database;
- modify configuration;
- modify lint configuration;
- create a commit.

---

# 26. Verification

The review MAY inspect existing source, schema and test evidence.

No implementation is required.

The current baseline should remain:

- Build: passing;
- Prisma validation: passing;
- Lint: 0 errors, 11 warnings;
- no source changes.

No warning reduction is expected.

---

# 27. Required Final Report

Copilot MUST return:

## Executive Decision

State whether the Reservation application/persistence contract is now sufficiently defined for implementation.

## Reservation Number

State its authoritative owner and persistence mapping.

## Booking Context

State the source of:

- customer;
- Booking status;
- currency;
- dates;
- amount.

## Traveller

State the exact mapping of `travellerSnapshots[]`.

## Lifecycle

State:

- authoritative lifecycle;
- Booking status role;
- supplier Reservation status role;
- physical representation.

## Save Context

State the exact context required for Reservation persistence.

## Retrieval Context

State the exact information required to reconstruct Reservation.

## Contract Matrix

Provide the completed reconciliation matrix.

## Remaining Dependencies

List only unresolved contract decisions.

---

# 28. Successful Outcome

The review succeeds when the active Reservation domain/application contracts provide a complete, unambiguous source for every value required by the physical Reservation model.

The desired outcome is:

**RESERVATION PERSISTENCE CONTRACT RECONCILED**

Only then may the next implementation specification be produced.

---

# 29. Blocked Outcome

If any required value has no authoritative source, report:

- the missing value;
- the current contract containing the gap;
- the required contract owner;
- the decision required.

Do not implement a workaround.

---

# 30. Completion Boundary

This document ends at contract reconciliation.

It does NOT authorise:

- Prisma changes;
- Reservation mapper changes;
- Reservation repository changes;
- domain changes;
- application changes;
- lint remediation;
- migrations;
- database changes.

The next document may be an implementation specification only after the contract reconciliation is accepted.

---

# 31. Final Status

**READ-ONLY RESERVATION CONTRACT RECONCILIATION**

No implementation is authorised.

Required progression:

3G Read-Only Contract Reconciliation
→ Accepted Contract Decisions
→ Focused Implementation Specification
→ Architect Approval
→ Copilot Implementation
→ Verification
→ Architect Acceptance
→ User Commit